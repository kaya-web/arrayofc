#include<stdio.h>
 int arrayLength(int*);
 int main()
 {
    int i,a[i];
    printf("enter value in array\n");
    for(i=0;i<=a[i];i++)
    {
        scanf("%d",a[i]);
    }
    arrayLength(a);


 }
 int arrayLength(int*a)
 {
    int i,len=0;
    for(i=0;i<=a[i];i++)
    {
        len++;
    }

    printf("%d",len);

 }

